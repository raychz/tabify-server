import { Injectable } from '@nestjs/common';
import { getRepository } from 'typeorm';
import { TicketItemUser } from '@tabify/entities';

@Injectable()
export class TicketItemUserService {
  constructor() { }
}